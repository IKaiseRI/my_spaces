public interface MyObserver {
    /**
     * Observer interface will be realised further in other classes.
     * It has only one method that accepts only one variable.
     * @param p
     */

    void update(double p);
}
